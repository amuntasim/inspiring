$('#stories_container').append('<%= j render(partial: "home/story", collection: @stories) %>');
<% if @stories.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @stories %>');
<% else %>
$(window).off('scroll');
$('.pagination').remove();
<% end %>
